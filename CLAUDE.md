# Français Quest · 项目工作日志

> 通用偏好见全局 `~/CLAUDE.md`。本文件只放项目专属信息。

## 项目简介
TCF Canada 备考个人学习管理工具。纯 HTML + CSS + Vanilla JS，Supabase 云同步可选。
运行方式：双击 `index.html` 或 `npx serve .`（端口 3456）

**核心文件：**
- `app.js` — 全部业务逻辑（~2800 行）
- `site-data.js` — 教材数据（~1250 行）
- `supabase.js` — 认证 + 云同步（~1450 行）
- `styles.css` — 样式（~660 行）

## 本项目工作流
- **复杂/不确定的改动** → 开新分身 + 新分支 → 满意后合并 main
- **简单小改动** → 主 worktree 的 main 上直接改、直接推
- 改完 UX 问题先截图验证再推送
- commit message 用中文，简明扼要

---

## UX 问题修复进度

### ✅ 已完成（严重问题）

**问题1 — 假入口**
- 题库中心、视频补充顶栏灰显 + 「建设中」角标
- 页面内加 🚧 横幅，移除视频补充页无效「解析」按钮

**问题2 — 首页/内页功能重叠 + 顺延改进**
- 首页任务卡：简化为只读+勾选，保留 ⋯ 菜单（顺延+补卡），移除添加/删除
- 自动顺延默认改为**开启**（原来默认关闭）
- 顺延任务显示橙色「↩ 顺延 N 次 · 原计划 X月X日」
- 本周视图：每条任务可勾选完成，可拖拽移天
- 本周视图：顺延任务在**原计划日**显示幽灵条目（⊘ 删除线 + 「↩ 已顺延至X月X日」），只显示最初 plannedDate，不重复

**问题3 — 错题逻辑不清**
- 错题复盘页默认展示错题池（列表优先）
- 手动录入改为「＋ 手动记录错题」折叠展开，有说明文字
- 练习答错后「✓ 已自动加入错题库」常驻显示在判题结果旁

---

### 🟠 待处理（中等问题）

按优先级排列，下次从第1条开始：

1. **单元进度 0%，无起点引导** — 第一个单元加「推荐起点」标签，空状态加引导提示
2. **「AI 建议」是静态文案** — 去掉「AI」字样，改为「每日学习提示」，或基于用户数据动态生成
3. **自动顺延引导** — 新用户首次进入时弹一次引导（已默认开启，但用户不知道可以关/改）
4. **「380天」倒计时无说明** — 加 hover tooltip「距 TCF Canada 考试还有 N 天」
5. **讲义/练习 Tab 和导出按钮混淆** — 导出按钮改为「⬇ 导出讲义 PDF」字样，与 Tab 拉开视觉距离
6. **练习组完成无成绩小结** — 每组题做完后展示小结卡片（共N题，正确M题，正确率X%）

### 🟡 待处理（轻微问题）

7. 原生日期选择器与像素风格割裂
8. 功能导览和顶部导航完全重复（首页左栏可改为数据摘要）
9. 面包屑导航层级不一致（「← 首页」vs「← 返回课程」）
10. 未登录状态无登录价值说明
11. 移动端完全不可用（< 720px 布局错位）

---

## 关键设计决策记录

| 决策 | 原因 |
|------|------|
| 首页任务卡只读+勾选，管理去内页 | 避免功能重叠，首页=总览，内页=操作 |
| 顺延幽灵只显示 plannedDate，不显示中间日期 | 避免重复，只需看到「最初计划」 |
| 自动顺延默认开启 | 对备考用户最有价值，默认关闭会被忽略 |
| 题库/视频保留导航入口但灰显 | 产品路线图可见，不误导用户 |
| 错题池优先展示，表单折叠 | 大多数错题自动入库，手动表单是例外 |

---

## 数据结构速查

**Task:** `{ id, title, type, done, date, plannedDate, workingDate, completedAt, rolloverCount, rolloverMode, detail }`
**Mistake:** `{ id, date, source, question, myAnswer, correctAnswer, reason, reflection, tags, mastered, reviewCount }`
**StudyLog:** `{ "YYYY-MM-DD": { done, total, manual } }`

localStorage keys: `fq-tasks-v3`, `fq-mistakes-v3`, `fq-study-log`, `fq-exam-date`, `fq-exercise-stats-v1`

---

*最后更新：2026-04-16*
